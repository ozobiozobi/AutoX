
declare let Autox = {
    ...root,
    java = javaModule,
    clipManager = clipManager,
    media = media,
    toast = toast,
    ui = ui,
    dialogs = dialogs,
    engines = engines,
    app = app,
    accessibility = accessibility
}